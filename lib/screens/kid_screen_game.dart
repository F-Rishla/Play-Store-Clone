import 'package:flutter/material.dart';

class KidScreen extends StatelessWidget {
  const KidScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Text("Kid asScreen")),
    );
  }
}
