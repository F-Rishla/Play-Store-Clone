import 'package:flutter/material.dart';

class TopChat extends StatelessWidget {
  const TopChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Text("tttop chat screen")),
    );
  }
}
